from data_structures.queue import Queue


def multi_bracket_validation(str):
    """Arguements: string -->
    Returns a boolean value to make sure that type of bracket
    `()`, `[]`, and `{}` has an opening and closing bracket."""
    pass
