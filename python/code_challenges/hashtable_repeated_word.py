from data_structures.hashtable import Hashtable


def first_repeated_word(string):
    """
    Arguments: string -->
    Returns: string -->
    Takes in a string and finds the first work to occur more than once in the string.
    """
    pass
